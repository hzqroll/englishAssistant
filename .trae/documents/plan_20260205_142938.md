I will redesign the `SplitAnalysisNotion.vue` page to strictly match the provided Notion-style HTML mockup and UI/UX prompts.

### 1. Layout Refactoring
- **Change Layout Structure**: Move from the current 12-column grid to a **3-Panel Flexbox Layout**:
  - **Panel 1 (Left, 30%)**: **Draft / Input**. Clean minimalist editor with a bottom action bar.
  - **Panel 2 (Middle, 35%)**: **Rule Engine**. A "Database View" style list of grammar errors and warnings.
  - **Panel 3 (Right, 35%)**: **Notion AI Results**. The AI rewrite, learning analysis, and tips.

### 2. Styling & Theming
- **Port Notion Variables**: Copy the specific CSS variables (colors for backgrounds, borders, tags, AI purple) from the HTML file into the Vue component's `<style>`.
- **Typography**: Apply the requested font stack (`Inter`, `Source Serif 4`, `Geist Mono`) using Tailwind classes (`font-sans`, `font-serif`, `font-mono`).
- **Icons**: Adapt the Lucide icons from the mockup to the project's existing `@heroicons/vue` library to maintain consistency without adding dependencies.

### 3. Component Implementation
I will implement the following UI elements within the Vue view:
- **NotionToggle**: Collapsible sections for error details and tips.
- **NotionCallout**: Colored boxes for summaries and insights.
- **NotionTag**: Pastel-colored status tags for error categories.
- **DatabaseTable**: A clean table view for error patterns.

### 4. Logic & State Integration
- **Preserve Demo Mode**: Ensure the existing "Demo Mode" logic (simulating API calls) works with the new UI elements.
- **Input Handling**: Use a clean `textarea` for the input panel (to ensure editing reliability) but styled to look like a borderless Notion block.
- **Store Connection**: Bind the `analysisStore` results (Rules and LLM) to the new Middle and Right panels respectively.

This redesign will result in a "Input → Rules → AI" flow that matches the visual hierarchy of the provided design files.