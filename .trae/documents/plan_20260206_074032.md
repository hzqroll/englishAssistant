为了彻底测试页面的所有按钮和交互，我将执行以下计划，确保覆盖所有关键组件的交互逻辑：

1. **执行现有测试基准**

   * 运行现有的前端测试套件，建立基准状态。

2. **新建** **`AIOptimizeButton`** **组件测试**

   * 创建 `src/components/panels/__tests__/AIOptimizeButton.spec.ts`。

   * **测试点**：

     * 验证四种状态（Disabled, Enabled, Loading, Completed）的正确渲染。

     * 验证点击 "AI 深度优化" 按钮是否触发 `optimizeWithLLM`。

     * 验证 Token 估算和实际消耗的显示。

3. **新建** **`LayoutControls`** **组件测试**

   * 创建 `src/components/layout/__tests__/LayoutControls.spec.ts`。

   * **测试点**：

     * 验证 Input, Compare, Analysis 面板切换按钮的点击事件。

     * 验证按钮的激活状态样式是否随 `panels` store 状态变化。

4. **新建** **`AnalysisPanel`** **组件测试**

   * 创建 `src/components/panels/__tests__/AnalysisPanel.spec.ts`。

   * **测试点**：

     * 验证 "搜索错误" 输入框的过滤功能。

     * 验证错误分类卡片的展开/折叠交互。

     * 验证 "学习建议" 卡片中的按钮（如果有）。

5. **增强** **`InputPanel`** **组件测试**

   * 更新 `src/components/panels/__tests__/InputPanel.spec.ts`。

   * **新增测试点**：

     * 验证 "清空" 按钮是否清空文本框内容。

     * 验证 "准确性优先" / "自然度优先" 模式切换按钮是否更新 `selectedMode`。

     * 验证 "导入文件" 按钮（即使目前是占位符，也应确认其存在）。

6. **验证与总结**

   * 运行所有测试，确保通过率 100%。

   * 输出测试覆盖的交互列表，确认满足"所有页面按钮都能被测试到"的要求。

