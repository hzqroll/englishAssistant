I will set up an automated testing framework (Vitest) for the frontend to allow you to "automatically debug" and verify the page functionality without manual testing.

### 1. Configure Testing Environment
-   **Update `package.json`**: Add a `test` script to run Vitest.
-   **Create `vitest.config.ts`**: Configure Vitest to work with Vue and alias paths (`@/`).

### 2. Create Unit & Component Tests
I will create test files to verify the fixes we just made and ensure core functionality works:
-   **`src/components/errors/__tests__/ErrorHighlight.spec.ts`**:
    -   Test `highlightedOriginal` logic: Ensure it correctly wraps original text with error spans.
    -   Test `highlightedCorrected` logic: **Crucial**. Verify it correctly reconstructs the corrected sentence using `originalText` + `correctedText` from errors (the fix we just implemented).
-   **`src/stores/__tests__/analysisStore.spec.ts`**:
    -   Test `analyzeText` action: Verify it correctly maps the backend `snake_case` response to the frontend `camelCase` state (the fix from the first step).
-   **`src/components/panels/__tests__/InputPanel.spec.ts`**:
    -   Verify input validation (min/max length).
    -   Verify "Analyze" button state (disabled when empty/analyzing).

### 3. Run Verification
-   Execute `npm test` to run all tests and confirm everything passes.

This setup will provide a permanent "automatic debugging" harness. Whenever you modify code, you can run `npm test` to instantly verify if the page logic is broken.
